# IP & DNS Lookup

A Next.js web application for performing IP and DNS lookups. Given an IP address or domain name, it returns geolocation data, DNS records, WHOIS information, and reverse DNS resolution.

This project was built as a sandbox to try out the [get-shit-done (GSD)](https://github.com/gsd-build/get-shit-done/) AI-assisted development workflow.
