---
phase: 02-core-ui
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-lookup.ts
  - src/hooks/use-lookup.test.ts
autonomous: true
gap_closure: true
requirements:
  - SRCH-01
  - SRCH-02
  - CARD-01
  - CARD-02
  - CARD-03
  - CARD-04
  - CARD-05
  - CARD-06
  - CARD-07
  - UI-01
  - UI-04

must_haves:
  truths:
    - "All 4 fetch URLs in performLookup use ?q= parameter, not ?query="
    - "Unit test explicitly asserts that fetch is called with ?q= in the URL"
    - "All existing unit tests continue to pass after the fix"
  artifacts:
    - path: "src/hooks/use-lookup.ts"
      provides: "Fixed fetch URLs with ?q= parameter"
      contains: "?q="
    - path: "src/hooks/use-lookup.test.ts"
      provides: "URL parameter contract test"
      contains: "?q="
  key_links:
    - from: "src/hooks/use-lookup.ts"
      to: "/api/geo, /api/dns, /api/rdns, /api/whois"
      via: "fetch calls with ?q= query parameter"
      pattern: "/api/(geo|dns|rdns|whois)\\?q="
---

<objective>
Fix the query parameter mismatch that causes all 4 API lookups to return 422 errors.

Purpose: The hook uses `?query=` in fetch URLs but all 4 API route handlers read `?q=`. This single bug blocks the entire phase goal — every lookup fails with INVALID_INPUT. Fixing this unblocks SRCH-02 and makes all cards display live data.

Output: Patched `use-lookup.ts` with correct `?q=` parameters and a new unit test asserting the URL parameter contract.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-ui/02-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit test asserting fetch URLs use ?q= parameter</name>
  <files>src/hooks/use-lookup.test.ts</files>
  <read_first>
    - src/hooks/use-lookup.test.ts (current test file — understand existing helpers and patterns)
    - src/hooks/use-lookup.ts (source under test — see current fetch URL construction at lines 107-112)
  </read_first>
  <behavior>
    - Test: performLookup('8.8.8.8') calls fetch with URLs containing `?q=8.8.8.8`, not `?query=8.8.8.8`
    - Assert each of the 4 URLs matches the pattern `/api/{endpoint}?q=`
    - Assert no URL contains `?query=`
  </behavior>
  <action>
Add a new test case at the end of the `describe('performLookup')` block in `src/hooks/use-lookup.test.ts`:

```
it('Test 7: fetch URLs use ?q= parameter (not ?query=)', async () => {
  // Minimal mock responses for all 4 endpoints
  mockFetch
    .mockImplementation(() => makeOkResponse({}))

  await performLookup('8.8.8.8')

  const urls = mockFetch.mock.calls.map((c: unknown[]) => c[0] as string)
  // Every URL must contain ?q= (the parameter API routes read)
  for (const url of urls) {
    expect(url).toContain('?q=')
    expect(url).not.toContain('?query=')
  }
  // Verify exact parameter format for each endpoint
  expect(urls).toContainEqual(expect.stringContaining('/api/geo?q=8.8.8.8'))
  expect(urls).toContainEqual(expect.stringContaining('/api/dns?q=8.8.8.8'))
  expect(urls).toContainEqual(expect.stringContaining('/api/rdns?q=8.8.8.8'))
  expect(urls).toContainEqual(expect.stringContaining('/api/whois?q=8.8.8.8'))
})
```

This test will FAIL (RED) because the current code uses `?query=` in all 4 URLs. The test must fail before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd /Users/nathanoger/Developpement/DevTest && npm test -- src/hooks/use-lookup.test.ts 2>&1 | grep -E "(FAIL|Test 7|✓|×|passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/use-lookup.test.ts contains the string `?q=` in a test assertion
    - src/hooks/use-lookup.test.ts contains the string `not.toContain('?query=')`
    - Test 7 FAILS when run against the unfixed source (RED phase of TDD)
    - Tests 1-6 still pass
  </acceptance_criteria>
  <done>New test case exists that asserts the URL parameter contract. It fails against the current buggy code (RED), confirming the test correctly catches the mismatch.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ?query= to ?q= in all 4 fetch URLs and verify GREEN</name>
  <files>src/hooks/use-lookup.ts</files>
  <read_first>
    - src/hooks/use-lookup.ts (the file being fixed — confirm exact lines 107, 108, 110, 112)
  </read_first>
  <action>
In `src/hooks/use-lookup.ts`, replace the 4 occurrences of `?query=` with `?q=` in the `performLookup` function (lines 107-112):

Line 107: change `/api/geo?query=${encodeURIComponent(sanitized)}` to `/api/geo?q=${encodeURIComponent(sanitized)}`
Line 108: change `/api/dns?query=${encodeURIComponent(sanitized)}` to `/api/dns?q=${encodeURIComponent(sanitized)}`
Line 110: change `/api/rdns?query=${encodeURIComponent(sanitized)}` to `/api/rdns?q=${encodeURIComponent(sanitized)}`
Line 112: change `/api/whois?query=${encodeURIComponent(sanitized)}` to `/api/whois?q=${encodeURIComponent(sanitized)}`

Each replacement is identical: the string `?query=` becomes `?q=`. No other changes to the file.
  </action>
  <verify>
    <automated>cd /Users/nathanoger/Developpement/DevTest && npm test -- src/hooks/use-lookup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/use-lookup.ts does NOT contain the string `?query=` anywhere
    - src/hooks/use-lookup.ts contains exactly 4 occurrences of `?q=`
    - All 7 tests pass (including the new Test 7 from Task 1)
    - grep -c '?query=' src/hooks/use-lookup.ts returns 0
    - grep -c '?q=' src/hooks/use-lookup.ts returns 4
  </acceptance_criteria>
  <done>All 4 fetch URLs use `?q=` matching the API route parameter. All 7 unit tests pass (GREEN). The parameter mismatch bug is resolved.</done>
</task>

</tasks>

<verification>
1. `grep -c '?query=' src/hooks/use-lookup.ts` returns 0 (no old parameter name remains)
2. `grep -c '?q=' src/hooks/use-lookup.ts` returns 4 (all 4 endpoints use correct parameter)
3. `npm test` passes with all tests green (both existing 6 + new Test 7)
4. `grep 'not.toContain.*query' src/hooks/use-lookup.test.ts` returns a match (contract test exists)
</verification>

<success_criteria>
- The string `?query=` does not appear in src/hooks/use-lookup.ts
- 4 fetch URLs use `?q=` parameter matching what API routes read via `searchParams.get('q')`
- A unit test explicitly asserts the URL parameter contract (prevents future regression)
- All existing + new tests pass: `npm test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ui/02-04-SUMMARY.md`
</output>
